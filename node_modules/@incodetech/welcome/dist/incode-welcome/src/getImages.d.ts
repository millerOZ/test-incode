type GetImagesParams = {
    token: string;
    body: {
        images: string[];
    };
    onDownloadProgress?: (progress: number) => void;
};
declare function getImages({ token, body, onDownloadProgress }: GetImagesParams): Promise<any>;
export default getImages;
