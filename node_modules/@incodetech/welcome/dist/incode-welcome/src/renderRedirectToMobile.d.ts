import { RedirectProps } from './redirect/Redirect';
declare function renderRedirectToMobile(element: Element, { onSuccess, session, flowId, logoSrc, url, showSms, allowReEnrollment, assets, expired, smsText, skipDesktopRedirect, allowSkipRedirect, externalId, }: RedirectProps): {
    close: (...props: unknown[]) => void;
};
export default renderRedirectToMobile;
