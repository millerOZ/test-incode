type AddSignatureParams = {
    token: string;
    image: string | Blob;
    type: string;
    sendBase64?: boolean;
};
declare function addSignature({ token, image, type, sendBase64, }: AddSignatureParams): Promise<any>;
export default addSignature;
