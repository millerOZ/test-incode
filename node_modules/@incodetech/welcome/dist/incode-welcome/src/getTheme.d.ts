export type CustomTheme = {
    buttonBorderRadius: string;
    buttonColor: string;
    main: string;
    subtitle: string;
    logo: string;
};
declare function getTheme(token: string): Promise<CustomTheme>;
export default getTheme;
