declare function addPhone({ token, phone, text, optInGranted, }: {
    token: string;
    phone: string;
    text?: string;
    optInGranted?: boolean;
}): Promise<import("axios").AxiosResponse<any, any>>;
export default addPhone;
