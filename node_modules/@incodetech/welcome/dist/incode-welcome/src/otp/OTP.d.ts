type OTPProps = {
    isReady: boolean;
    interviewCode: string;
    timer: number;
    onSubmit: (otp: string) => void;
    error: boolean;
    onCodeChange: (otp: string) => void;
    code: string;
    generatingCode?: boolean;
};
declare function OTP({ isReady, interviewCode, timer, onSubmit, error, onCodeChange, code, generatingCode, }: OTPProps): import("react/jsx-runtime").JSX.Element;
export default OTP;
