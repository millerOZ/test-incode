import type { LooseSession } from '../../../types/src';
type RenderRetryStepsProps = {
    token: LooseSession;
    numberOfTries: number;
    showPassport: boolean;
    showTutorials?: boolean;
    idTimeout: number;
};
type RenderRetryStepsCallbacksProps = {
    onSuccess: () => void;
    onError: () => void;
};
declare function renderRetrySteps(element: any, { token, numberOfTries, showPassport, showTutorials, idTimeout, }: RenderRetryStepsProps, { onSuccess, onError }: RenderRetryStepsCallbacksProps): void;
export default renderRetrySteps;
