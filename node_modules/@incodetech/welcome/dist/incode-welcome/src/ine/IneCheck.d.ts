import { LooseSession } from '../../../types/src';
type IneCheckProps = {
    goNext: () => void;
    backgroundExecution?: boolean;
    token: LooseSession;
    flow?: any;
};
declare function IneCheck({ goNext, backgroundExecution, token, flow }: IneCheckProps): import("react/jsx-runtime").JSX.Element;
export default IneCheck;
