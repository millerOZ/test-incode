import type { LooseSession, OnboardingStatus } from '../../../types/src';
type UseQRProps = {
    session: LooseSession;
    onSuccess: ({ onboardingStatus, }: {
        onboardingStatus: OnboardingStatus;
    }) => void;
    url?: string;
    allowReEnrollment?: boolean;
    expired?: boolean;
    externalId?: string;
    lang?: string;
    canceled?: boolean;
};
declare const useQR: ({ session, onSuccess, url, allowReEnrollment, expired, externalId, canceled, lang, }: UseQRProps) => {
    redirectUrl: string;
};
export default useQR;
