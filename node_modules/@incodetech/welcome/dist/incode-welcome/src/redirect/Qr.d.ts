import type { OnboardingStatus, Session } from '../../../types/src';
export type QrProps = {
    onSuccess: (res: {
        onboardingStatus: OnboardingStatus;
    }) => void;
    session: Session;
    url?: string;
    primaryColor?: string;
    secondaryColor?: string;
    sizePx?: number;
};
declare function Qr({ session, onSuccess, url, primaryColor, secondaryColor, sizePx, }: QrProps): import("react/jsx-runtime").JSX.Element;
export default Qr;
