import type { LooseSession, OnboardingStatus } from '../../../types/src';
export type RedirectProps = {
    onSuccess: (res: {
        onboardingStatus: OnboardingStatus;
    }) => void;
    session: LooseSession;
    flowId?: string;
    logoSrc?: string;
    url?: string;
    showSms?: boolean;
    allowReEnrollment?: boolean;
    assets?: {
        logo: string;
        background: string;
    };
    expired?: boolean;
    smsText?: string;
    skipDesktopRedirect?: () => void;
    allowSkipRedirect?: boolean;
    externalId?: string;
};
declare function Redirect({ session, onSuccess, url, showSms, assets, smsText, skipDesktopRedirect, allowSkipRedirect, externalId, expired, allowReEnrollment, }: RedirectProps): import("react/jsx-runtime").JSX.Element;
export default Redirect;
