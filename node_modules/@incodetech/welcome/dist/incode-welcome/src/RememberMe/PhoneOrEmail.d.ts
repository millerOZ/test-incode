import type { LooseSession } from '../../../types/src';
import { FC } from 'react';
type PhoneOrEmailProps = {
    session: LooseSession;
    onSuccess: (response?: any) => void;
};
declare const PhoneOrEmail: FC<PhoneOrEmailProps>;
export default PhoneOrEmail;
