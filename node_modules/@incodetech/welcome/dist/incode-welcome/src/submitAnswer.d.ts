type Answer = {
    interviewId: string;
    questionId: string;
    answerDetails: {
        singleAnswer?: string;
        selectedAnswers?: string[];
    };
};
export default function submitAnswer({ token, answer, }: {
    token: string;
    answer: Answer;
}): Promise<any>;
export {};
