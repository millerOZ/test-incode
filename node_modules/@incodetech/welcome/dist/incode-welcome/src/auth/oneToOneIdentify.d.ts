import type { FaceCoordinates } from '../detection/detectFace';
type OneToOneIdentityProps = {
    base64Image: string;
    phone?: string;
    email?: string;
    faceCoordinates: FaceCoordinates;
    authReference?: string;
    uuid?: string;
    nationalNumber?: string;
    videoRecordingId?: string;
    videoRecordingToken?: string;
};
declare function oneToOneIdentify({ base64Image, phone, email, authReference, uuid, nationalNumber, videoRecordingId, videoRecordingToken, }: OneToOneIdentityProps): Promise<any>;
export default oneToOneIdentify;
