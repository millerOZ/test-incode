import type { FaceCoordinates } from '../detection/detectFace';
type IdentityProps = {
    base64Image: string;
    faceCoordinates: FaceCoordinates;
    authReference: string;
    videoRecordingId?: string;
    videoRecordingToken?: string;
};
declare function identify({ base64Image, faceCoordinates, authReference, videoRecordingId, videoRecordingToken, }: IdentityProps): Promise<any>;
export default identify;
