import type { FaceCoordinates } from '../detection/detectFace';
type IdentityProps = {
    token: string;
    base64Image: string;
    faceCoordinates: FaceCoordinates;
};
declare function identifyInstantVerify({ token, base64Image, faceCoordinates, }: IdentityProps): Promise<any>;
export default identifyInstantVerify;
