import { LoginProps } from './types';
export type IdentifyResponse = {
    spoofAttempt: boolean;
    faceMatch: boolean;
    token: string;
    customerId: string;
    interviewId: string;
    base64Image: string;
    secondFactor: boolean;
    transactionId: string;
    hasLenses: boolean;
    hasFaceMask: boolean;
    error: {
        timestamp: number;
        status: number;
        error: string;
        message: string;
        path: string;
    };
};
export default function Login({ onError, onSuccess, onCapture, timeout, stopAtError, stopAtAccessDenied, isOneToOne, oneToOneProps, accessDeniedNotificationText, token, isInstantVerify, showCustomCameraPermissionScreen, isKiosk, recordingsToken, hatCheckEnabled, lensesCheckEnabled, maskCheckEnabled, eyesClosedCheckEnabled, }: LoginProps): import("react/jsx-runtime").JSX.Element;
