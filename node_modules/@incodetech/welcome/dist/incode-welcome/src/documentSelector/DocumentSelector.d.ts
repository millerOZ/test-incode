import { LooseSession } from '../../../types/src';
import { FC } from 'react';
export type DocumentSelectorProps = {
    goNext: (source: string) => void;
    showManualUpload?: boolean;
    session: LooseSession;
};
declare const DocumentSelector: FC<DocumentSelectorProps>;
export default DocumentSelector;
