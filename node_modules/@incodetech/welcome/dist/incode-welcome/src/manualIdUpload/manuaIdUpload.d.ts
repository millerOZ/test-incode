import React from 'react';
import { LooseSession } from '../../../types/src';
interface Props {
    token: LooseSession;
    onSuccess: () => void;
    onlyFront?: boolean;
}
declare const CaptureManualId: React.FC<Props>;
export default CaptureManualId;
