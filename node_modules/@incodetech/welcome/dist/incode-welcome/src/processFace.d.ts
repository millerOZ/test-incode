/** Response from the face processing endpoint */
type ProcessFaceRespone = {
    /** Confidence score between 0 and 1, where 0 means no match and 1 means perfect match */
    confidence: number;
    /** Indicates if the user exists in our database */
    existingUser: boolean;
};
type ProcessFaceParams = {
    token: string;
    imageType?: 'selfie' | 'videoSelfie';
};
declare function processFace({ token, imageType }: ProcessFaceParams): Promise<ProcessFaceRespone>;
export default processFace;
