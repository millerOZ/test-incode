declare function createSession(_countryCode: string, externalId?: string, { externalCustomerId, configurationId, uuid, interviewId, customFields, language, forceProductionMode, }?: {
    externalCustomerId?: string;
    configurationId?: string;
    uuid?: string;
    interviewId?: string;
    customFields?: object;
    language?: string;
    forceProductionMode?: boolean;
}): Promise<import('../../types/src').Session>;
type CreateSessionError = {
    status: number;
    message: string;
};
export default createSession;
export type { CreateSessionError };
