import type { Session } from '../../types/src';
declare function createNewSession({ externalId, externalCustomerId, configurationId, uuid, interviewId, customFields, language: _language, shouldInitialize, forceProductionMode, }?: {
    externalId?: string;
    externalCustomerId?: string;
    configurationId?: string;
    uuid?: string;
    interviewId?: string;
    customFields?: object;
    language?: string;
    shouldInitialize?: boolean;
    forceProductionMode?: boolean;
}): Promise<Session>;
type CreateNewSessionError = {
    status: number;
    message: string;
};
export default createNewSession;
export type { CreateNewSessionError };
