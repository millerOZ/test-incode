import type { LooseSession } from '../../../types/src';
type RenderFiscalQrProps = {
    onSuccess: () => void;
    session: LooseSession;
};
declare function renderFiscalQr(element: Element, { onSuccess, session }: RenderFiscalQrProps): void;
export default renderFiscalQr;
