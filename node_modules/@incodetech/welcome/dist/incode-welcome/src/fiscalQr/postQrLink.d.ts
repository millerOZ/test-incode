import type { LooseSession } from '../../../types/src';
export type Success = {
    result: string;
    resultDetails: string;
    success: boolean;
};
type PostQrLinkProps = ({ session, link, }: {
    session: LooseSession;
    link: string;
}) => Promise<Success>;
declare const postQrLink: PostQrLinkProps;
export default postQrLink;
