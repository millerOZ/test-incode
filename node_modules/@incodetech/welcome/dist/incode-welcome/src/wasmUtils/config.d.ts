import { WasmPipelineType } from './wasmPipelineType';
import { PipelineProfile } from './pipelineProfile';
import { DetectionType } from '../detection/detectionType';
interface InitializationParams {
    wasmModulePath: string;
    wasmSimdModulePath: string;
    glueCodePath: string;
    versionsFilePath: string;
    pipelineModels: Map<WasmPipelineType, Array<string>>;
    enableSIMD: boolean;
    profiles: Map<DetectionType, PipelineProfile>;
    wasmVersion: string;
}
export declare function getInitializationParams(): InitializationParams;
export {};
