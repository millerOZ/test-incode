import type { LooseSession } from '../../types/src';
type AddDocumentParams = {
    token: LooseSession['token'];
    type: 'contract' | string;
    image: string;
    onProgress?: (progress: number) => void;
};
declare function addDocument({ token, type, image, onProgress, }: AddDocumentParams): Promise<any>;
export default addDocument;
