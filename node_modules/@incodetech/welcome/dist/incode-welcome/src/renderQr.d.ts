import type { Session } from '../../types/src';
type RenderQrProps = {
    onSuccess: () => void;
    session: Session;
    url?: string;
    primaryColor?: string;
    secondaryColor?: string;
    sizePx?: number;
};
declare function renderQr(element: Element, { onSuccess, session, url, primaryColor, secondaryColor, sizePx, }: RenderQrProps): void;
export default renderQr;
