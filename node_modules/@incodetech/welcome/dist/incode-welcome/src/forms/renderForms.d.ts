import { Screen } from './types';
type Options = {
    onSuccess: () => void;
    onError: () => void;
    token: string;
    flowId?: string;
    interviewId: string;
    screens: Screen[];
};
declare function renderForms(element: Element, { onSuccess, onError, token, interviewId, flowId, screens }: Options): void;
export { renderForms };
export default renderForms;
