import { Screen as ScreenType } from './types';
type ScreenProps = {
    screen: ScreenType;
    onContinue: (values: {
        [key: string]: unknown;
    }) => Promise<void>;
    isLastScreen: boolean;
    order: number;
    token: string;
};
declare function Screen({ screen, onContinue, isLastScreen, order, token, }: ScreenProps): import("react/jsx-runtime").JSX.Element;
export { Screen };
export default Screen;
