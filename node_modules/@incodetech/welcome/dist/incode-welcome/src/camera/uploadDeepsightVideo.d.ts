import type { Session } from '../../../types/src';
export declare const uploadDeepsightVideo: (video: Blob, token: Session['token']) => Promise<{
    recordingId: string;
}>;
export default uploadDeepsightVideo;
