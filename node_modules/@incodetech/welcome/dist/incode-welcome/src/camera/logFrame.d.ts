export declare const sources: {
    readonly front: "FRONT_ID";
    readonly back: "BACK_ID";
    readonly selfie: "SELFIE";
};
type Keys = keyof typeof sources;
type SourcesType = (typeof sources)[Keys];
export declare const logFrame: ({ source, base64Image, token, }: {
    source: SourcesType;
    base64Image: string;
    token: string;
}) => Promise<void | {
    success: boolean;
}>;
export {};
