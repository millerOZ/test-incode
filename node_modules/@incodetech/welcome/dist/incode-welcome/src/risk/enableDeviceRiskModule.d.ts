import type { LooseSession } from '../../../types/src';
type EnableDeviceRiskModuleParams = {
    token: LooseSession['token'];
    interviewId: string;
    deviceRiskClientId: string;
    isSandbox?: boolean;
};
export declare const enableDeviceRiskModule: ({ token, isSandbox, interviewId, deviceRiskClientId, }: EnableDeviceRiskModuleParams) => Promise<any>;
export {};
