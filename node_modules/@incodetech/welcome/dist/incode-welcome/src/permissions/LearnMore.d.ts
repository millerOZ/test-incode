import type { FC } from 'react';
type LearnMoreProps = {
    onAllowPermissions: () => void;
    emitError: (error: string) => void;
};
declare const LearnMore: FC<LearnMoreProps>;
export default LearnMore;
