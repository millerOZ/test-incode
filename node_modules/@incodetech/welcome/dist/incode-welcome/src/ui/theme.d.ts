import type { CustomTheme } from '../getTheme';
export type Theme = {
    button: {
        background: string;
        text: string;
    };
    accentColor: string;
    logo: string;
};
export declare const defaultTheme: Theme;
export declare const mergeTheme: (propsTheme?: Partial<Theme>, dashboardTheme?: CustomTheme) => Theme;
