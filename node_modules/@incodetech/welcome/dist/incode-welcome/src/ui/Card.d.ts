import { FC, ReactElement } from 'react';
interface CardProps {
    image: ReactElement;
    alt?: string;
    title: string;
    body: string;
    fullImageSize?: boolean;
}
declare const Card: FC<CardProps>;
export default Card;
