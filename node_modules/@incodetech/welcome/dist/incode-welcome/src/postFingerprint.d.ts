type PostFingerprintParams = {
    hash: string;
    ip: string;
    deviceType: string;
    token: string;
    data: string;
    osVersion: string;
    deviceModel: string;
    browser: string;
    sdkVersion: string;
    hasLiedBrowser: boolean;
    hostingApp: string;
    ipLocation?: {
        ipCountry: string;
        ipRegion: string;
        ipCity: string;
        ipLatitude: number;
        ipLongitude: number;
    };
};
declare function postFingerprint({ hash, ip, deviceType, token, data, osVersion, deviceModel, browser, sdkVersion, hasLiedBrowser, hostingApp, ipLocation, }: PostFingerprintParams): Promise<{
    success: boolean;
    sessionStatus: string;
}>;
export default postFingerprint;
