import { MetadataUtils } from '../metadata/metadataUtils';
import { PipelineProfile } from '../wasmUtils/pipelineProfile';
import { DetectionType } from './detectionType';
export interface CameraLabelInspectionResult {
    isDetected: boolean;
    hasPriorDetection: boolean;
}
declare class OnboardingAnalytics extends MetadataUtils {
    private motionStatusCheck;
    private estimatedPerformance;
    private analysisStatus;
    private motionStatus;
    private pipelineLogs;
    private simdIsEnabled;
    private wasmVersion;
    private profiles;
    private isVirtualCameraDetectedByLabel;
    private metadata;
    private static instance;
    static getInstance(): OnboardingAnalytics;
    constructor();
    setBackgroundMode(isBackgroundMode: boolean): void;
    setSimdIsEnabled(simdIsEnabled: boolean): void;
    setWasmVersion(wasmVersion: string): void;
    setProfiles(profiles: Map<DetectionType, PipelineProfile>): void;
    getPipelineLogs(): object;
    getMotionStatus(): string;
    getAnalysisStatus(): string;
    getMetadata(): string;
    analyzeFrame(fullImage: string): Promise<void>;
    updateParams(stream: MediaStream | null): Promise<void>;
    /**
     * Asynchronously checks if the provided video track or any device in the system is a virtual camera.
     *
     * @param {MediaStreamTrack | null} videoTrack - The video track to inspect for a virtual camera label. If null,
     * the method checks all device capabilities instead. Defaults to null.
     * @returns {Promise<CameraLabelInspectionResult | null>} A promise that resolves to a `CameraLabelInspectionResult`
     * indicating whether a virtual camera is detected and if there was a prior detection. The promise may resolve to
     * null if the ML Wasm API is not initialized or if an exception occurs.
     */
    checkForVirtualCameraByLabel(videoTrack?: MediaStreamTrack | null): Promise<CameraLabelInspectionResult | null>;
    startOnboardingCheck(): Promise<void>;
    estimatePerformance(): string;
    update(): void;
}
declare const _default: OnboardingAnalytics;
export default _default;
