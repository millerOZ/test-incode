type AddMLConsentParams = {
    token: string;
    mlConsentChecked: boolean;
    id: string;
};
declare const addMLConsent: ({ token, mlConsentChecked, id }: AddMLConsentParams) => Promise<{
    success: boolean;
}>;
export default addMLConsent;
