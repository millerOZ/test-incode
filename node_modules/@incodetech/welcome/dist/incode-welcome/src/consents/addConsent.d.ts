import type { LooseSession } from '../../../types/src';
type AddConsentReturnData = {
    success: boolean;
    sessionStatus: 'Alive' | 'Closed' | 'Deleted';
};
declare function addConsent({ title, content, status, session: { token }, }: {
    session: LooseSession;
    title: string;
    content: string;
    status: boolean;
}): Promise<import("axios").AxiosResponse<AddConsentReturnData, any>>;
export default addConsent;
