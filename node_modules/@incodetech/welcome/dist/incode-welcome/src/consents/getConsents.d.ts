import type { Consent, LooseSession, RegulationTypes } from '../../../types/src';
export type GetPrivacyConsenProps = {
    session: LooseSession;
    type?: string;
    regulationType?: RegulationTypes;
};
declare const getPrivacyConsentData: ({ session: { token }, type, regulationType, }: GetPrivacyConsenProps) => Promise<{
    consents: Consent[];
}>;
export default getPrivacyConsentData;
