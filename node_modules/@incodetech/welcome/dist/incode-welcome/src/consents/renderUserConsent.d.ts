import type { LooseSession } from '../../../types/src';
declare function renderUserConsent(element: HTMLElement | null, { session, onSuccess, title, text, isAeroMexicoClient, }: {
    session: LooseSession;
    onSuccess: () => void;
    title?: string;
    text?: string;
    isAeroMexicoClient?: boolean;
}): void;
export default renderUserConsent;
