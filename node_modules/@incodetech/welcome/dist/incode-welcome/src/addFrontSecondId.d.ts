import { emptyFunc } from '../../incode-utils/src';
declare function addFrontSecondId({ token, image, onProgress }: {
    token: any;
    image: any;
    onProgress?: typeof emptyFunc;
}): Promise<any>;
export default addFrontSecondId;
