import type { IncodeOptions, IncodeTheme } from '../../types/src';
import type { AxiosRequestConfig } from 'axios';
export declare const initializeDetection: ({ interviewId, forceProductionMode, }?: {
    interviewId?: string;
    forceProductionMode?: boolean;
}) => Promise<void>;
type SetupParams = {
    forceProductionMode?: boolean;
    theme?: IncodeTheme;
    clientId?: string;
    lang?: string;
    translations?: {
        [key: string]: string;
    };
    apiKey?: string;
    apiURL: string;
    /** @deprecated This parameter is no longer used. The apiKey is now received from backend responses */
    tokboxApiKey?: string;
    options: IncodeOptions;
    customHeaders?: AxiosRequestConfig['headers'];
    fingerprintApiKey?: string;
    skipInitialize?: boolean;
};
declare const setup: ({ forceProductionMode, theme: _theme, clientId, lang, translations, apiKey, apiURL, tokboxApiKey, options, customHeaders, fingerprintApiKey, skipInitialize, }: SetupParams) => Promise<void>;
export default setup;
