import type { LooseSession } from '../../types/src';
declare function approve({ token }: LooseSession): Promise<{
    customerId: string;
    totalScore: 'OK' | 'WARN' | 'MANUAL_OK' | 'FAIL' | 'UNKNOWN' | 'MANUAL_FAIL';
    existingCustomer: boolean;
}>;
export default approve;
