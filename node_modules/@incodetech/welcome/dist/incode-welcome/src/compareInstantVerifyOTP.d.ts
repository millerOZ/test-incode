type CompareValidationProps = {
    token: string;
    code: string;
};
declare function compareInstantVerifyOTP({ token, code, }: CompareValidationProps): Promise<any>;
export default compareInstantVerifyOTP;
