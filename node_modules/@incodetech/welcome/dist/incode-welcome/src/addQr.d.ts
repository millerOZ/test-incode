import type { LooseSession } from '../../types/src';
type AddQRResponse = {
    qr: string;
    customerId: string;
};
export default function addQr({ token }: LooseSession): Promise<AddQRResponse>;
export {};
