type StartInfo = {
    phonePrefix: string;
    country: string;
    ipIsoCode: string;
};
type GetStartInfo = (token: {
    token: string;
}) => Promise<StartInfo>;
declare const getStartInfo: GetStartInfo;
export default getStartInfo;
