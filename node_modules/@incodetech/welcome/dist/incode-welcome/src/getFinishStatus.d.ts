declare const getFinishStatus: (flowId: string | undefined | null, { token }: {
    token: string;
}) => Promise<{
    redirectionUrl: string;
    action: 'approved' | 'rejected' | 'none';
}>;
export default getFinishStatus;
