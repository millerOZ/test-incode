import { emptyFunc } from '../../incode-utils/src';
declare function addAddressStatement({ token, file, image, docType, format, onProgress, }: {
    token: any;
    file: any;
    image: any;
    docType?: string;
    format?: string;
    onProgress?: typeof emptyFunc;
}): any;
export default addAddressStatement;
