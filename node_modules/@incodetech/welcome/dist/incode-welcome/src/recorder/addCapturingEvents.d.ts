import type { LooseSession } from '../../../types/src';
export declare function addCapturingEvents({ currentMode, session, }: {
    currentMode: 'front' | 'back';
    session: LooseSession;
}): void;
