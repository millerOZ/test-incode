import type { LooseSession } from '../../../types/src';
import type { WebcamRef } from '../camera/Webcam.types';
type Props = {
    webcam: WebcamRef;
    onSuccessfulCapture: () => void | Promise<void>;
    mode: 'front' | 'back';
    session: LooseSession;
    onError: (message: string) => void;
    config: {
        shouldCompareOCR: boolean;
        shouldCompareID: boolean;
        numberOfTries: number;
    };
};
export declare const VideoSelfieIdCapture: ({ webcam, onSuccessfulCapture, mode, onError, session, config: { shouldCompareOCR, shouldCompareID, numberOfTries }, }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
