import type { LooseSession } from '../../../types/src';
import { videoSelfieEvents } from '../hooks/useModuleEvents';
declare const videoSelfieMode: {
    readonly selfie: "selfie";
    readonly captureId: "captureId";
};
type VideoSelfieEvents = keyof typeof videoSelfieEvents;
type VideoSelfieMode = keyof typeof videoSelfieMode;
export declare const logVideoSelfieEvent: ({ event, type, mode, session, }: {
    event: VideoSelfieEvents;
    type: VideoSelfieMode;
    mode: 'front' | 'back';
    session: LooseSession;
}) => void;
export {};
