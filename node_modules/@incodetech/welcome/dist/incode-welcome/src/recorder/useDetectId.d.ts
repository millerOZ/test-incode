import type { WebcamRef } from '../camera/Webcam.types';
type Mode = 'front' | 'back';
export declare const WAIT_FOR_MESSAGE = 3000;
export declare const useDetectId: ({ webcam, mode, numberOfTries, }: {
    webcam: WebcamRef;
    mode: Mode;
    numberOfTries: number;
}, callbacks: {
    onCapturing: ({ currentMode, image, }: {
        currentMode: Mode;
        image: string;
    }) => void | Promise<void>;
    onSuccessfulCapture: () => void | Promise<void>;
    onFarAway: () => void;
    onGlare: () => void;
    onBlur: () => void;
    onNoAttemptsLeft: (message: string) => void | Promise<void>;
}) => {
    status: "error" | "success" | "idle" | "capturing";
    message: string;
};
export {};
