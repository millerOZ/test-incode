type Consent = {
    id: string;
    question: string;
    answer: string;
    language: string;
};
export type Consents = {
    consents: {
        'en-US': Consent;
        'es-ES': Consent;
    };
};
declare const getVideoSelfieConfig: ({ session: { token }, }: {
    session: {
        token: string;
    };
}) => Promise<Consents>;
export default getVideoSelfieConfig;
