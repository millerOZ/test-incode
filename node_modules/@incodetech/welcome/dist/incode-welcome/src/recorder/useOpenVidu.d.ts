type Status = 'connecting' | 'recording' | 'stopped' | 'error' | 'streamDestroyed';
export declare const useOpenVidu: ({ token }: {
    token: string;
}) => {
    publishAndStartRecording: (stream: MediaStream) => Promise<void>;
    stopRecording: () => Promise<void>;
    isOnline: boolean;
    status: Status;
};
export {};
