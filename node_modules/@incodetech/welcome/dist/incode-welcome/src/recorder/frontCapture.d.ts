import type { LooseSession } from '../../../types/src';
export declare function frontCapture({ image, session, shouldCompareOCR, shouldCompareID, t, }: {
    image: string;
    session: LooseSession;
    shouldCompareOCR: boolean;
    shouldCompareID: boolean;
    t: (key: string) => string;
}): Promise<void>;
