import type { LooseSession } from '../../types/src';
type AddContractParams = {
    token: LooseSession['token'];
    image: Blob;
};
declare function addContract({ token, image }: AddContractParams): Promise<{
    url: string;
    contractId: string;
}>;
export default addContract;
