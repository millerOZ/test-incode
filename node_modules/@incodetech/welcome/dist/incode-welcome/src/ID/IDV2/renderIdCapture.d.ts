type Props = {
    flowId: string;
    token: string;
    element: HTMLElement;
    onSuccess?: () => void;
    onError?: () => void;
};
declare function renderIdCapture({ token, element, onSuccess, onError }: Props): {
    close: (...props: unknown[]) => void;
};
export default renderIdCapture;
