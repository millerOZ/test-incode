export type Mode = 'front' | 'back' | 'passport';
type CaptureIdProps = {
    initialMode?: Mode;
    switchToBack?: () => void;
};
declare const CaptureId: ({ initialMode, switchToBack }: CaptureIdProps) => import("react/jsx-runtime").JSX.Element;
export default CaptureId;
