type UseCaptureCounterProps = {
    isCapturing: boolean;
    onReadyToCapture: () => void;
    initialCount?: number;
};
export declare function useCaptureCounter({ isCapturing, onReadyToCapture, initialCount, }: UseCaptureCounterProps): {
    counter: number;
    showFlash: boolean;
};
export {};
