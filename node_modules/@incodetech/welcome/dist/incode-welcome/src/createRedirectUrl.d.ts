import type { LooseSession } from '../../types/src';
type CreateRedirectUrlType = {
    session: LooseSession;
};
declare const createRedirectUrl: ({ session }: CreateRedirectUrlType) => Promise<any>;
export default createRedirectUrl;
