import type { LooseSession } from '../../../types/src';
export type WatchListProps = {
    token: LooseSession;
    onSuccess: () => void;
    onBoarding?: {
        processGlobalWatchlist: ({ token }: {
            token: string;
        }) => void;
    };
};
