type AddWatchListResults = {
    token: string;
    firstName: string;
    surName: string;
    birthYear?: number;
    countryCodes?: string[];
};
declare function addWatchListResults({ token, firstName, surName, birthYear, countryCodes, }: AddWatchListResults): Promise<any>;
export default addWatchListResults;
