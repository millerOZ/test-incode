import type { FC } from 'react';
import type { SendFaceImageError } from '../../api/sendFaceImage';
type ErrorModalProps = {
    error: SendFaceImageError['message'] | null;
    onTryAgain: () => void;
    onContinue: () => void;
};
export declare const ErrorModal: FC<ErrorModalProps>;
export {};
